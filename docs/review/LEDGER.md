# dl-finance-workbench — review rounds

Every round of the adversarial review loop, so the rounds can be compared. The protocol —
what the loop is, the binary verdicts, the independence rules and the stop condition — is
`vendor/demo-kit/packages/review/REVIEW.md`.

The loop stops when every reviewer returns yes, or when the severity rule says another round
would buy nothing: a Critical always buys one, a High buys one up to round four, a Low buys
none, three rounds is the floor and five the ceiling. `demo review --record` prints which
applied. This file is the loop's memory — the rule reads it to tell a loop that is converging
from one that is a treadmill, because the same gap appearing three rounds running is a
different problem from three rounds each finding something new.

Rounds are opened by `demo review` and filled in by `demo review --record`. Editing this
file by hand is fine and expected — add a line saying what changed between two rounds.

## Round 1 — 2026-08-17 — ea60c30

| Reviewer | Verdict |
| --- | --- |
| cpo | — no reply yet |
| ceo | — no reply yet |
| domain | — no reply yet |
| user | — no reply yet |
| deck | no |

Blocking gaps:

- `CRITICAL` **deck** — Slide 3 ("Why Now") says two incompatible things about what the workbench does, on one page, and one of them has to be false. The standfirst: "What no part of it does is connect the pieces, interpret the movement and explain it to a board — which is exactly the work a governed layer on top can now take on." The diagram directly beneath it puts "Variance, forecast, scenario and the commentary a board reads — **still a person, every month**" in the *Decision* layer, tagged "Where the work is" — and places the workbench in the layer *below* that, tagged "Missing". So the sentence claims the layer takes on the interpreting and explaining, and the picture says that work stays with a person and the workbench sits under it. This is the slide that carries the entire value proposition, and I cannot tell from it whether I am buying the thing that does the work or the thing that sits beneath the thing that does the work. I would need the diagram and the standfirst to agree on which row the product occupies and which row stays manual, and the foot to say plainly what the product removes from a person.
- `HIGH` **deck** — The problem the deck opens with is not the problem the deck then solves, and no slide bridges the two. Slide 2 names the pain as a five-step manual cycle run twelve times a year: export, rebuild the model, rebuild the pack, rebuild the commentary. Slides 4, 5 and 6 show one thing: a figure interrogated — position, bridge, drill to rows. Nothing in them produces a model, a pack or a commentary. The only step of the five they touch is the last one, "a question". As it stands the deck argues "your monthly rebuild is the problem" and then demonstrates a lookup tool. I would need either a problem slide framed around the questions rather than the rebuild, or a surface that shows the pack or the model produced rather than queried.
- `HIGH` **deck** — I am asked to spend from US$185K and twelve weeks and the deck never states a return, a target, or a unit of value. The roadmap says a baseline is taken in week one and week twelve is "measured against the week-one baseline" — but the deck never says what is measured. Days off the close? Fewer people? Fewer restatements? Hours of board Q&A? Compare the credentials slide, which is happy to quote "72h to 15min" for someone else's programme. Nothing equivalent is offered for this product. I would need one sentence naming the measure and a plausible target, even a range, so I know what a successful week twelve looks like before I agree to fund it.
- `HIGH` **deck** — There is no evidence anywhere that this product has ever touched a real ledger, and the deck does not admit it. Every screenshot is Kestrel Industrial Group plc — declared synthetic, once, on slide 3, and to the deck's credit declared plainly. Every credential on slide 8 is for different work: data estates and forecasting platforms at ADCB and EWEC. None of it is a finance close, and none of it is this product. So the deck's proof section proves the team, not the workbench. There is no named finance function, no pilot, no reproduced close, no reference I can call. The deck asks me to be the first customer without ever saying I would be the first. I would need it stated: either name who is running it, or say plainly that this is the first deployment and price the risk accordingly.
- `HIGH` **deck** — "Twelve surfaces" is claimed once, on the title slide, and never substantiated. Three are shown. The other nine are never named, listed, or dated. Worse, the three shown make nearly the same argument — "every figure opens into the working behind it" (slide 4) and "each figure drills to the rows that made it" (slide 6) are the same sentence. On a skim I see one capability demonstrated three times, not a twelve-surface product. I would need the twelve named on a single line somewhere, and the three chosen to show three different things.
- `HIGH` **deck** — The commercial shape is missing. I cannot form a budget from this. "From US$185K" — no upper bound, and no statement of what makes it more. Nothing about what happens after week twelve: licence, subscription, hosting, support, per-entity or per-user cost. The workbench is described as a product I run my close on; a product I run my close on has a running cost, and this deck has none. The roadmap says what I keep if I stop at week three (the data-quality assessment and the baseline). It never says what I own at week twelve — the model? the code? a tenant? I would need a year-one total, or at minimum the line items that make one up.
- `HIGH` **deck** — The section "Why Now" does not answer why now. Its headline — "Keep the accounting system. The gap is the layer above it." — answers *what the gap is*, which slide 2 already told me. The only "now" in the slide is the word itself ("can now take on"), asserted and never explained. Nothing dates the opportunity: no regulation, no cost pressure, no technology that became possible recently. If there is no reason this is a 2026 conversation rather than a 2022 one, the section should be renamed to what it actually does.
- `HIGH` **deck** — Nothing on security, hosting, data residency or access, for a product whose first act is to ingest five entities' general ledgers. The roadmap mentions "the controls your audit function asks for" and "permissions matching who owns which entity", both inside weeks 9–11 — i.e. after I have already loaded my ledgers in weeks 1–3. No statement of where the data sits, whose cloud, which jurisdiction, what certifications, or who at Deeplight can see my group's unpublished numbers. My IT and audit functions will ask this before I get to page two, and the deck hands me nothing to answer them with.
- `HIGH` **deck** — The credentials are unsourced numbers about unrelated work, and there is nothing about the company itself. "Analytical processing 72h to 15min · data quality 67% to 95% · 2,000+ users" — no definition of "data quality", no date, no attestation, no indication whether the client would confirm any of it. "1,300+ models deployed inside client programmes over ten years" — "model" is undefined and the count is a vanity number; ten years also sits oddly against a company introduced by two named engagements. Deeplight's own facts are entirely absent: how many people, how long trading, how many clients, financial standing. I am being asked to put my monthly close on a supplier and the deck tells me four names and two logos. A DIFC company with two Gulf references, selling to a £-reporting plc, also needs to say something about serving a group like mine.
- `LOW` **deck** — Slides 8 and 9 contradict each other on whether I can stop. The roadmap says the twelve weeks are "stoppable at every step"; the Discovery card on slide 9 says Discovery is "the one engagement you can stop at the end of". Both cannot be true, and the one that matters to me is the one attached to the $185K.
- `LOW` **deck** — The contents page promises sections that never arrive, and the dividers are misnumbered. Contents lists "Our Team" and "Engagement" under The Company; no slide carries either eyebrow (the engagement slide's eyebrow reads "How We Work"). Contents numbers The Solution 02, but its divider reads 03 — and the Company divider also reads 03. Two dividers numbered the same on a deck asking for six figures reads as carelessness.
- `LOW` **deck** — Slide 6 buries the deck's single most important qualification. "For this to hold on your own data, the comparator has to be a versioned artefact rather than a spreadsheet somebody keeps" is the one line that tells me whether any of this works in my company — and it sits at the end of a hundred-word paragraph that has already been through vintages, restatement policy, comparator naming and rounding conventions. The rounding point in particular ("nothing is rounded before it is compared") is an engineering detail a CFO will not weigh, and it is crowding out the thing I need to read.

_Open: 1 of 5 replies recorded. A round with a missing verdict is not a round._

_Round 1 was cut off, not abandoned. The console's session hit the 600-second
background-task ceiling and the harness killed the reviewers mid-round. `deck` is the one brief
forbidden the running app, so it did not have to wait on it and its findings above are real and
complete. The four product verdicts were never written. Re-run round 1; do not read this as a
round that came back four times silent._

## Round 2 — 2026-08-17 — 8384b6e

**Round 2 was run twice, concurrently, by two runs of the skill in one working tree.** This run
(`claude -p /demo-kit-review`, pid 94237, started 14:00) opened round 2, ran five reviewers,
recorded `5 no — 14 critical, 32 high, 7 low`, and fixed. A second run (pid 63532, started 14:13)
was executing the same skill against the same checkout: it re-emitted these briefs and its own
reviewers wrote over the same five `.reply.md` paths. `cpo.reply.md` was replaced at 15:13 and
`user.reply.md` at 15:23 — both after this run had already recorded the round and committed
against it. The two runs also shared `/tmp/demo-review-dev.pid`, which is why the dev server pid
went missing mid-round and had to be recovered from `ps`.

So the tally above is a true reading of five complete, independent, correctly-shaped replies as
they stood when `--record` read them, and it is **not** reproducible from the files now on disk.
Every fix committed under round 2 was separately verified against the code before it was made —
each one names the file and line it corrects — so the fixes stand on their own evidence rather
than on the bookkeeping. What cannot be trusted is the round's provenance. The loop stopped here
rather than opening round 3 or deploying: a round is only worth what its independence is worth,
and two runs writing one round have no independence to measure.

| Reviewer | Verdict |
| --- | --- |
| cpo | no |
| ceo | no |
| domain | no |
| user | no |
| deck | no |

Blocking gaps:

- `CRITICAL` **cpo** — The Commentary surface — stop 9 of 12, and the one surface that proves the product can do the work the deck's problem slide is built on — produces ungrammatical English in every paragraph, and this is the state the demo ships in keyless. The README's claim that without a key "the demo is whole" is not true. I would have to see seven paragraphs a group financial controller could paste into a board pack unedited before I believed the deck's central claim that this removes the commentary rewrite. Seven paragraphs, seven broken sentences: "Against Forecast it £618k higher", "Against Forecast it £588k higher", "Against Forecast it 194bps lower", "Against Forecast it £94k higher", "Against Forecast it £64k lower", "Against Forecast it £92k lower", "Against Forecast it £1.1m lower". The verb is missing from the template, not from one instance. `web/lib/story.ts:159` — `` `Against ${against.label} it ${said(against.movement, against.unit)}` ``. Every period, every entity, every comparator inherits the defect. The deck sells this exact page twice. Slide 2: "The pack, and the commentary explaining it. Written from scratch every month." The guided tour's own stop 9 calls it "A concise Board-ready statement". It is not board-ready; it is not sentence-ready. Same class of defect in the Overview's entity notes: "not every posting carries **a** entity", printed under all four measures.
- `HIGH` **cpo** — Two surfaces state materially different 13-week cash forecasts for the same group, the same month and the same approved basis, and neither reconciles to the other. The product exists to stop exactly this. I would have to see one 13-week line, or both pages naming their opening balance and basis on the face of the table, before I would put this in front of a treasurer. `/app/cash`: "Opening balance £4.8m … Week 9 closes at £1.7m, £760k under the £2.5m floor. Its low point is £1.3m in week 10." Funding panel: "£7.9m can arrive in time against a shortfall of £760k." `/app/scenarios`, row labelled **"Approved forecast"**: "LOW POINT £2.2m · HEADROOM −£273k · BREACH Week 10", and the decision card reads "The floor is breached in week 10 by £273k, and £8.8m can be reached in time." I reached the second by clicking the first. The Overview raises "Cash breaches the floor by £760k in week 9" as a HIGH risk whose named action is "Stress the cash floor"; that link lands on the page that says £273k in week 10. The number I clicked from is nowhere on the page I arrived at. The cause is a deliberate basis difference — `web/lib/scenario.ts:354` runs plan-against-plan off forecast cash (£5.9m) while the Cash surface runs off actual cash (£4.8m) — which makes it worse, not better: the code comment at line 419 asserts that "'fund it by week seven' on this page and on the cash page are the same claim", and the running product proves it is not.
- `HIGH` **cpo** — There is no user evidence of any kind, and I could not find a trace of one. Every input to this product is the vendor's own. That is the finding, not a caveat. The source material named in `docs/review/00-source-review.md` is `DEEPLIGHT_Finance_Workbench_PRD_120826.docx` and `DEEPLIGHT_Finance_Transformation_AI_120826.pptx` — Deeplight's own PRD and Deeplight's own slides. The review's own words: "the findings are all about what neither says." I grepped the entire `docs/` tree for interview, user research, design partner, pilot customer, "we asked", "spoke to". Zero hits. The deck's credentials slide is two data-platform programmes (ADCB, EWEC) and 1,300+ models. None of it is this product, and none of it is a finance close. The deck offers no CFO who has seen this, no close it has been run against, and no number about what a close costs today. I would have to see three named group CFOs or financial controllers who have driven this for twenty minutes each, with what they said, and at least one of them a person who then asked when they could have it. Absent that, everything in it is a hypothesis about a buyer, expressed very fluently.
- `HIGH` **cpo** — Only one month of the demo is alive, and the failure is silent. Every period other than July 2026 reads +0.0% against the default comparator, the MATERIAL flags disappear and the four decision boards empty out to "No headline movement cleared the materiality policy this period." The first thing a CFO does with a variance product is look at last month. Driven, all on the default `vs Forecast v6`: Jun 26 revenue +0.0% / EBITDA +0.0% / cash +0.0%; May 26, Apr 26, Mar 26, Dec 25 all +0.0% / +0bps on all four measures. `/api/v1/measures?month=2026-06` returns `comparative` equal to `value` to the penny on three of the four measures. The Explore grid prints the whole of it in one screen: six columns Feb–Jul 2026, and only the July column has a non-zero variance on any row. The model can do better and the UI will not let you reach it. `packages/measures/src/comparator.ts` hardcodes `choice.versionId ?? 'v6'`; v4 and v5 exist with earlier `actualsThrough`, and `/app?month=2026-04&version=v4` gives a full live variance (−3.8% revenue, −326bps margin, −19.7% EBITDA). But the version picker exists only on `/app/forecast`, and the context bar on every other surface prints "Comparator: vs Forecast v6" with no way to change it. I would have to see the comparator resolve to the forecast version that was in force when the period opened, or, failing that, the page say on its face why a variance is zero. A blank decision board with no explanation reads as a broken product, and in a demo it reads as one month of stagecraft.
- `HIGH` **cpo** — The deck says "The workbench is built" on the only slide that carries a schedule and a price, and never says what is not built. Everything missing sits inside the fixed twelve weeks and the US$185K. Not built, on the evidence of the running product and its own copy: any ERP or consolidation connector (deck slide 7 puts these in weeks 9–11); persistence of any kind ("Nothing a visitor does persists on this tier", "this tier stores nothing"); the approval workflow the product repeatedly names — "the commentary workflow is draft, review, approve, publish, and this tier stores nothing"; authentication, since a persona is a URL parameter (`?as=gulf-controller`). The problem slide's whole argument is "nothing above the ledger can read it in place". The part that reads the ledger in place is the part that does not exist. I would have to see one slide, or one line on slide 7, that says plainly what exists today and what week 12 has to create. As it stands the reader prices a product and buys an integration project, and I would not sign that for a team of mine.
- `HIGH` **cpo** — Twelve surfaces is at least eight too many for what the case needs, and the extra ones are where the incoherences live. The argument gets stronger under the cut, which is the tell. The case that lands is one spine: position → bridge → drill to rows, plus 13-week cash. That is four surfaces (Overview, Performance, Cash, Explore) and it is genuinely good. I would cut KPIs, Year to Go, Capex & Procurement, People and Forecast Quality before funding it. Every one of them is another surface to keep coherent with the other eleven, and the two coherence breaks I found are both between surfaces I would have cut or between one of them and the spine. Scenarios I would keep only because the lever genuinely re-runs the generator — but it is also the source of finding 2. The deck already knows this: it shows three surfaces and mentions twelve. If three carry the hour, the other nine are cost, not proof.
- `HIGH` **cpo** — Nothing anywhere says what this is worth, so there is no case against doing nothing. Doing nothing is what I would do. The deck quantifies the problem as "twelve times a year, the same work" and stops. No days, no FTE, no cost of an argued figure, no cycle time. The one measurable thing on offer is a baseline that the buyer pays to have taken in week one — the engagement discovers whether the engagement was worth it. What it competes with, and none of it is addressed: the spreadsheet (free, already trusted, no integration project); the group FP&A analyst who already writes the commentary in two days; and the incumbent, which is doing nothing for another quarter. The deck's own layer diagram concedes BI and planning are "already solved", which means the buyer already has a place to look at numbers. I would have to see one number of the form "the July close took N days and M of them were spent reconstructing figures somebody already had", taken from a real finance function — and then this product's claim on N.
- `HIGH` **cpo** — Week seven, this becomes a thing someone opens when asked. I do not think it survives, and I want to be specific about why rather than gesture at novelty wearing off. There is no write path. No approval, no publish, no persistence, by construction. So the close still happens in the existing tools and this is a second place to look, which is one more thing to reconcile rather than one fewer. Week-seven products that add a reconciliation die. The one thing that removes work is the commentary, and the commentary does not produce publishable prose (finding 1). The demo's coherence is calibrated on one month with one planted set of conditions — two unmapped codes, one restatement, one intercompany break, one three-month subcontract run. In week seven the data has grown and the conditions are different ones, and I have no evidence the product handles a month where nothing interesting happened other than by going blank (finding 4). What would have to be true for it to still be in use: the commentary is good enough to publish from, the approval and publish steps exist so the pack is produced here rather than copied out of here, and the comparator behaves on any month a user picks.
- `CRITICAL` **ceo** — The last screen of the guided tour is a variance grid that reads `+0.0%` four months in a row. The demo's central claim is that it explains variance against a versioned forecast. Against Forecast v6 that variance exists in exactly one month. Every other month the comparator resolves to the same number as the actual, so the product tells the reader the forecast was correct to the penny — repeatedly, in a wide table, on the surface the tour uses to close. No CFO reads that as a forecast; they read it as data that was made up, and once they have read it that way, the eleven honest surfaces before it are retroactively suspect. I would have to see the Forecast comparator either refuse a period that sits inside the forecast's own actuals window and say in one sentence why there is nothing to compare, or resolve to the forecast version that was actually in force when that month closed, so a closed month carries a real variance. What I cannot approve is a demo whose flagship number is demonstrably degenerate one click away from the opening screen. Step 12 of 12, `/app/explore?...&measure=ebitda` — the tour's own default row and the tour's own default surface. Rendered: `2026-02 +0.0% · 2026-03 +0.0% · 2026-04 +0.0% · 2026-05 +0.0%`. `/api/v1/measures?month=` for 2026-01 through 2026-06: revenue, EBITDA and cash all return `movement: 0` with `comparative` identical to the actual to the minor unit. July 2026 is the only month with a full set of movements; June has one (gross margin, and that is the planted £310k reclassification, not a forecast miss). The May position page states it out loud: "No headline movement cleared the materiality policy this period, so none is broken down." The product knows the period is empty and shows it anyway. This also makes the README's claim that the period selector "genuinely recompute[s] rather than relabel[s]" only half true. The actuals recompute. The comparator collapses.
- `CRITICAL` **ceo** — I cannot say in one sentence what I am approving, because the deck asks for three different things and pre-selects one of them. Slide 10 asks for an hour and a written verdict. Slide 9 tags "Accelerate — From US$185K, fixed scope, fixed price, twelve weeks" with the label "This deck". Slide 7 presents those twelve weeks as a plan already agreed ("This is what it takes to get it running on **your** ledgers"). An hour is a diary entry; US$185K and twelve weeks of my controllers' time is a decision. I would have to see one sentence, on the ask slide, naming exactly what is being bought, at what price, by whom, and what I keep if I stop — and every other slide subordinate to it. This is the cheapest finding here to fix and the most likely reason a good demo gets a no.
- `CRITICAL` **ceo** — I was asked to approve the whole cost and the artefact contains one number. "From US$185K" is the only price anywhere in the deck, and I could find no cost model of any kind in the repository either — no run rate, no year two, no per-close or per-seat, no hosting, no per-use model cost, no support model, no integration cost per source, no headcount, no maintenance. Two of the three routes the deck offers (Discovery, People) carry no price at all. Meanwhile the demo itself sets the obligation: nine configured feeds including SAP S/4HANA universal-journal extraction, Oracle Fusion BI Cloud Connector, D365 lake link, NetSuite REST, Anaplan, Salesforce, a PSA, payroll and camt.053 bank statements; 393 load vintages; IAS 21 translation across four currencies; and a roadmap that puts "ERP and consolidation connections, permissions, the controls your audit function asks for, monitoring on the pipelines, and the controllers trained on it" into three weeks. I would have to see the cost of one customer for three years — build, integrate, run, support, audit — beside what they pay over the same three years. Until that exists, the question I was handed has no answer, and "from US$185K" reads as the number that makes the slide work rather than the number that makes the business work. Searched `docs/plan/*.md` for price, licence, per-seat, run-rate, TCO, ACV, market size: nothing. The plan documents are engineering-only and thorough; the commercial half is absent.
- `HIGH` **ceo** — There is no ceiling in this, and the one claim the ceiling rests on is the one a buyer will contest first. Nobody is named as the buyer beyond "a group CFO", there is no count of them, no segment, no contract value, and no statement of what the company looks like if this wins. Worse, the load-bearing slide says the finance layer is "Missing" while listing "Planning tools hold the budget" as already solved — and Anaplan, a planning tool, is a source feed inside the demo. OneStream, Workday Adaptive, Pigment, Vena, Board and Oracle EPM all sell "the governed layer above the ledger" to exactly this buyer. The deck does not mention that they exist. If the honest answer is that the ceiling is twelve-week engagements sold by four named people, then this is a services business with a very good demo, and I need to be told that in those words rather than infer it from a slide that says "From US$185K". I would have to see: who buys, how many exist, what they pay in year one and year three, and one paragraph on why the incumbent in their stack has not already done this.
- `HIGH` **ceo** — The slide headed "Why Now" does not contain a why-now. It contains a layer-cake argument — keep the ERP, the gap is above it — that was equally true and equally buildable in 2019, and the only justification offered is "which is exactly the work a governed layer on top *can now take on**". Nothing is named that became ready. The product does have a genuine why-now — a model that can phrase board commentary and answer questions while being held to governed tools and refused when it cannot ground an answer — and the deck never mentions it once across ten slides, while the demo I was given has it switched off. So the one thing that is new about this is absent from both halves of what I was shown. I would have to see the why-now be a specific capability with a date on it, and see that capability working.
- `HIGH` **ceo** — The board-commentary surface, which is the product's prose output, is ungrammatical in every one of its seven paragraphs, and the copy underneath boasts about it. Each paragraph reads "Against Forecast it £618k higher, which is ahead of where it was expected to be" — the verb is missing, seven times, on the surface whose promise is that a CFO can take the sentence to a board. Immediately below sits the line "Code writes these paragraphs, not a model," which turns a typo into a claim about quality. Separately, the same sentence gets cost polarity backwards in words: "Cost of sales ... £588k higher, which is **behind** where it was expected to be" reads as an underspend when it is an overrun, and the same wording hits Overheads. I would have to see all seven paragraphs read as English a controller would paste unedited, and cost lines described as above or below their expectation rather than ahead or behind it. `web/lib/story.ts`: `said()` returns a bare "£618k higher" for the non-zero case while returning "is unchanged"/"is not comparable" for the others — so the defect fires only on the paragraphs that actually have news, which is all of them in July.
- `HIGH` **ceo** — Nobody outside the building has been asked, and the references are for a different buyer. This product takes ownership of the materiality policy, the mapping set, the reconciliation checks and the figures a board reads. That needs a yes from the external auditor, from group financial control whose judgement it encodes, from a CISO signing off read access to a live SAP universal journal across five entities, and from whoever owns the ICFR control narrative. None of them is named anywhere. And every credential offered is UAE regulated banking and energy — ADCB, EWEC, CBUAE, ADGM, 1,300+ models — against a demo that is a UK plc with sterling presentation, IAS 21 translation and board minutes. There is no UK, EU or listed-industrial reference at all, so the first CFO who sees this is being asked to be the first of their kind while looking at a slide about a Gulf bank. I would have to see, for each of those approvers, what they would say when shown exactly this, and one reference that looks like the buyer.
- `HIGH` **ceo** — Slide 5 prints a guarantee that the roadmap on slide 7 quietly contradicts, and nobody has costed it. Slide 5: "£0k unattributed — every bar named, the residual among them, and the parts sum to the movement exactly." That holds in the demo because the world comes from one fixed seed. Slide 7's week 4–8 is honest about the real case — back-test against closes the controllers already stood behind, "naming where it reproduces them and where it does not" — which concedes that it may not reproduce them. Both cannot be the promise. On a real ledger with real intercompany, real accruals and real restatements, an exactly-zero residual every month is an accuracy obligation with a cost attached and a liability behind it. I would have to see the exact-tie claim stated as a property of this synthetic group rather than of the product, and the obligation costed wherever it is offered as one.
- `LOW` **ceo** — The deck's own numbering contradicts its contents page. The contents lists The Solution as 02, and the divider that opens it is labelled 03 — the same label as the divider that opens The Company. The contents also promises "Our Team" and "Engagement" sections, and no slide carries either eyebrow ("How We Work" is where Engagement should be). A reader flicking back to find a section will not find it where the contents said it was.
- `LOW` **ceo** — The flagship input still carries the template's words. The Ask box on Explore reads "Ask about a site, a month, or a comparison". There are no sites in this product; it is a finance workbench over five legal entities. It is verbatim from `vendor/demo-kit/template/web/components/Ask.tsx`, and it is the single most-looked-at field on the surface the tour ends on.
- `LOW` **ceo** — Figures in the same column disagree about scale. On the opening position page the EBITDA-by-entity table reads `+£115k`, `−£102k`, `−£71k`, then `−£5,886.36`. The commentary balance sheet reads `+£366.31` beside `−£1.5m`. The evidence chain mixes `+£335k` with `+£6,491.34` and `−£45.23` in one column. The rule behind it is deliberate and documented in `packages/measures/src/units.ts` — exact figures below £10k, because rounding £4,182 to £4k is a rounding nobody asked for — and I think the rule is right in isolation and wrong inside a column of thousands. The `scale` option that would pin a column already exists and is not used on these tables.
- `CRITICAL` **domain** — The scenario engine gets the *sign* of the cash answer wrong on the downside case, and cancels the group's liquidity problem because of it. This is the question every FP&A demo lives or dies on, and it is answered the wrong way round. `/app/scenarios?volume=0.9` — volume 10% below plan for the rest of FY26. FY26 revenue −£7.5m, EBITDA −£2.8m, net income −£2.3m, and **cash +£1.5m, net debt −£1.5m**. The "Headroom against the floor" panel on the same page goes from "Approved forecast: low point £2.2m, headroom −£273k, breach Week 10" to "Scenario: low point £3.3m, headroom **+£811k**, breach **None**." Losing a tenth of the group's volume is reported as removing its cash-floor breach. The same page simultaneously recommends "Take a fixed-cost reduction to the board" and "Freeze recruitment outside delivery roles" because EBITDA is 20.4% down. It is telling the CFO to cut costs and telling the Treasurer the funding problem has gone away. The mechanism is that the generator carries receivables, inventory and payables as functions of the revenue level, so a lower run rate produces a permanently smaller book with no transition cost and no fixed-cost drag on cash. A sustained volume loss releases working capital **once** and destroys cash every month after that. What I would have to see: a downside volume case in which cash deteriorates; or, if a working-capital release genuinely offsets part of it, that release shown as its own one-off line with the run-rate cash effect stated separately — and the floor panel must not report "None" on the strength of a one-off.
- `CRITICAL` **domain** — The product says the £212k of unmapped ledger value is outside the reported profit and loss. It is inside it. Its own reconciliation check proves the opposite. Overview, Adverse board: "Until they are mapped that value is outside the reported profit and loss, so the figures are complete against the mapping and not against the ledger." The same sentence is in the control alert on `/app/controls` as the "risk if unreviewed". Performance, Operating expense table: "Unmapped operating expense **£212k**", inside "Total operating expense £3.1m", which is what `consolidate.ts:313` subtracts to get EBITDA (`opex = staff_cost + other_opex + unmapped_opex`). The £212k is in reported EBITDA. `checks.ts:113-122` reconciles the unmapped P&L line to the mapping register and passes — i.e. the product formally proves the reported figures **are** complete against the ledger, then tells the reader on the front page that they are not. These are two different exceptions with two different owners and two different remedies. An unmapped account that drops out of the reported numbers is a **completeness** failure and goes to the audit committee. One that lands in a reported suspense line is a **classification** failure and goes to the mapping owner. The product does the second and describes the first, so a controller acting on it would size the risk wrongly and escalate to the wrong place. What I would have to see: the sentence saying that the value is reported but unclassified, and which reported line is holding it. As a second-order point, £148k of it is "58420 Subcontract labour", which in this model belongs in cost of sales, not opex — so the group gross margin is flattered by roughly 119bps against the same 194bps miss the product is asking the board to discuss, and nothing connects the two findings.
- `CRITICAL` **domain** — The £212k is seeded into Forecast v6 *and* Budget FY26 at exactly £212k, so its variance is £0.00 and the exception is invisible on the surface built to find it. Performance, all three comparators driven: vs Forecast v6 — actual £212k, comparator £212k, variance **+£0.00, +0.0%**. vs Budget FY26 — the same. vs Prior year — correctly "—". The EBITDA bridge therefore draws a bar labelled "Unmapped opex **£0k**", so the £212k contributes nothing to the EBITDA variance the whole surface exists to decompose. This asserts that the FY26 budget, approved before the year opened, provided £212,000 against ledger accounts that had not yet been created. `seed.ts:946` states the intended rule in terms — "a forecast cannot have failed to map an account that had not appeared when it was made" — and then the plan versions are generated through the same function with the same flag (`seed.ts:2122-2136`), so the rule is not applied. What I would have to see: nil in every plan version, and a −£212k unmapped-opex bar in the EBITDA bridge.
- `CRITICAL` **domain** — The receivables ageing on the flagship cash surface is not a possible ageing, and it depicts a group in collections distress that no other surface acknowledges. `/app/cash`: group receivables £24.2m, DSO 65 days, and the book aged as Current £3.5m (14.5%), 31–60 £7.6m, 61–90 **£9.9m (41%)**, over 90 £3.1m — "Overdue £20.6m · **85.4%**". Monthly external revenue is £12.4m. A book of £24.2m is under two months of billing, so essentially all of the last month and most of the one before it must sit inside 60 days. Only £3.5m current against £12.4m of billing in the month is arithmetically impossible unless invoicing collapsed, which it did not. The cause is in `funding.ts:349-353`: the weights are `exp(-|centre - dso| / spread)` over centres [15, 45, 75, 120], which centres the *mode* of the distribution on the DSO. DSO is the *mean age** of a book that decays monotonically from Current; it is never the peak bucket. At 65 days this puts 41% of the book in 61–90. What this then claims about the business: 85% of a £24.2m book past 30-day terms and £3.1m over 90 days is a material IFRS 9 expected-credit-loss event and a covenant conversation. There is no bad-debt provision or impairment account in the taxonomy, no detector fires on it, and the only receivables item on the board is "Kestrel Gulf collections 12 days slower". Meanwhile `cash.ts:190` collects the whole book inside the 13-week horizon regardless. The page's disclosure — "The totals are governed; the distribution is not" — is honest about provenance but does not cover this: the number published beside it, 85.4% overdue, is a finding a credit controller would act on, and it is not a shape this ledger could produce. What I would have to see: an ageing that decays from Current, with the Current bucket approximately one month's billing; and, if a book really were 85% overdue, a provision line and a detector.
- `CRITICAL` **domain** — The July close is dated in early July, three weeks before July ends, while the ledgers that produced it loaded on 4 August. `/app/controls`, Close readiness · Jul 2026: all four closed entities show Submitted *2026-07-04 09:00** and Closed **2026-07-06 17:00**. Kestrel Inc's expected close is **8 Jul**, and the Overview says "the expected close is past the group's **6th**". The Loads and vintages table on the same surface has `v-2026-07-core` loading **2026-08-04 03:12 UTC**, which is the correct timing. The period is closed a month before its data arrives. `seed.ts:1607-1608` hard-codes `submittedAt: ${month}-04` and `closedAt: ${month}-06`, i.e. day 4 and day 6 *of the period itself* rather than of the following month. This is the demo's own headline governance story — "4 of 5 ledgers closed, period not final" — and it is the first thing the person in my brief reads. It also breaks the narrative it carries: by 4 August, when the loads landed, an 8 July expected close is four weeks overdue, not the "two working days after the group's own close date" the code claims. What I would have to see: submissions and closes dated in the month following the period, on the same calendar as the load register.
- `HIGH` **domain** — Two 13-week cash forecasts in the same product disagree on the flagship treasury number, and both are stated as fact about the group. `/app/cash` and the Overview risk board: "The 13-week forecast first breaches the floor in *week 9**, closing at £1.7m, **£760k** under the £2.5m floor... low point £1.3m in week 10." `/app/scenarios` base case, nothing moved: "Approved forecast — low point **£2.2m**, headroom *−£273k**, breach **Week 10**." The difference is the opening balance — actual July cash £4.8m on one page, Forecast v6's July cash £5.9m on the other — but the scenarios panel names no basis for its own row, so a treasurer reading both in one session has two different breach weeks and two different shortfalls for the same group and horizon. What I would have to see: one number, or each stated with the basis it runs off.
- `HIGH` **domain** — There is no equity, no non-controlling interest, and the "blocking" balance-sheet identity check cannot fail for three of the five entities. Kestrel Europe GmbH is 85% owned and fully consolidated. `consolidate.ts:247-251` computes a non-controlling interest, and it is never surfaced anywhere — no measure, no balance-sheet line, no P&L attribution. The only place a user learns a minority exists is a hand-typed sentence in `funding.ts:91` ("Non-controlling interest at 15%"), which is prose rather than the 0.85 the model holds. So the PAT series on the Performance chart and the "Profit after tax £1.3m" paragraph in the board commentary are 100% of a partly-owned subsidiary's profit with no owners/NCI split. For a plc that is the number the market reads, and it is the split every group pack carries. Worse, `consolidate.ts:219-231` defines the translation reserve for each foreign entity as `assets − liabilities − share capital − retained earnings`, i.e. exactly the plug that makes that entity balance. The three foreign entities therefore cannot contribute a difference, so the check billed as blocking with a £0.00 threshold can only ever detect an imbalance in the two sterling entities — and the reserve labelled "Cumulative translation reserve" is silently absorbing the un-attributed minority interest as well as the translation difference. This is the failure the codebase itself warns against elsewhere: "a reconciliation that always balances is one that has stopped being a check." What I would have to see: NCI as an equity component and as a P&L attribution; a translation reserve computed from equity movements rather than as a residual; and a balance sheet with both sides on it.
- `HIGH` **domain** — "Eliminations and unattributed" attributes two subsidiaries' trading performance to the Group Financial Controller as a consolidation adjustment. Overview, Revenue by entity: Manufacturing +£285k, Services +£167k, Gulf +£149k, then "**Eliminations and unattributed +£17k**, 2.8%, Group Financial Controller". Europe moved +£82k and Inc +£40k (both visible on the Performance page). So £122k of named subsidiary performance and roughly −£105k of genuine elimination effect are netted into one line whose label says "elimination" and whose owner is the Group FC. The reader is told the consolidation adjustment helped revenue by £17k when it cost £105k, and two local controllers' variances have been booked to group. `DriverPanel.tsx:100-113` and `contributors.ts:206` confirm it: the residual row is the truncated tail of the ranking *plus* the elimination, with a fixed label and a fixed owner. The whole product's thesis is that every movement carries the owner held on its dimension. This is the one line where it does not, and it is on the first screen. What I would have to see: elimination as its own row, the untruncated entities as their own rows or an honestly labelled "other entities" row with no owner claimed.
- `HIGH` **domain** — The product asserts causes and owners it has not established, by hard-coding them over the model's own dimensions. `/app/scenarios?volume=0.9` — only the volume lever moved, and the implied decision reads "Hold price on renewal rather than defend volume — Gross margin is 81bps below the approved forecast, *from the price side**", owned by the Commercial Director. `decisions.ts:197-218` reaches that conclusion as the *else* branch of a test on the two cost levers: if the cost levers did not move, the cause is declared to be price. Nothing about price moved. The compression is operational gearing. The page's own preamble boasts that decisions are "triggered by what the outcome broke rather than by which lever moved" — this is the opposite, and it sends a pricing instruction to the wrong director. `detectors.ts:404` hard-codes `owner: 'Operations Director'` on the segment-margin finding, so the flagship adverse item — "Projects margin 404bps behind forecast" — is routed to Operations, while `taxonomy.ts` SEGMENTS names **Projects Director** as the owner of that segment and Services Director for Service contracts. What I would have to see: a cause the arithmetic supports or no cause claimed, and owners read from the dimension the model already holds them on.
- `HIGH` **domain** — No facilities, no covenants, no lease liabilities. The treasury surface is built around a constraint a group like this does not manage against. The group carries **£15.6m of drawn borrowings** (Commentary balance-sheet panel) and the entire 13-week exercise is measured against a £2.5m *cash* floor, with the answer to a £760k week-9 shortfall being a local board resolution in Dubai to move £193k. A group with a facility does not do that; it draws on the revolver. There is no undrawn-facility figure, no available liquidity, and no covenant — no net debt/EBITDA, no interest cover, no test date — anywhere in the 55-measure catalogue. Two entities are **overdrawn** (Services −£1.6m, Inc −£2.0m) and the group's headline "Cash & equivalents £4.8m" is the net of those against UK balances. A US overdraft cannot be offset against UK cash; that is £2.0m of borrowing presented as cash on the front page. The funding table shows them as "could send £0.00" rather than as overdrawn. The group's *actual* 30 June cash was £1.4m — £1.1m below the board floor — and nothing anywhere flags it. The floor monitor only looks forward. What I would have to see: available liquidity rather than cash as the constraint, at least one covenant with a test date, overdrafts shown as borrowings, and the floor tested against the position just closed as well as the forecast.
- `HIGH` **domain** — In the state I was given it, the demo does not stand up in front of a group financial controller, so the README's "without it the demo is whole" does not hold. Board commentary is the keyless deliverable, and every one of its seven paragraphs is ungrammatical: "Against Forecast it £618k higher", "Against Forecast it 194bps lower", and so on for all seven. A CFO handed that as "the sentence code wrote" stops reading at the first line. Ask produced **no answer in six attempts** — three typed and submitted, three via the suggestion chips. On two the page appeared to reload and the question vanished with nothing rendered; on one it sat on "Working on your question" for 20 seconds and then cleared. `POST /api/ask` returns `{"kind":"unavailable","reason":"The answer service is not running here..."}` instantly, and `Ask.tsx:110-140` is written to render exactly that — but that sentence never reached the screen once. "Explore & Ask" is guided-tour step 12 and one of the deck's three headline surfaces. I am not reporting the fallback as a crash. I am reporting that the fallback did not appear. What I would have to see: the seven commentary sentences grammatical, and the "cannot look anything up" message actually rendered on every submission.
- `CRITICAL` **user** — Every assumption lever on the Scenarios page 404s, including inside the guided tour's own step 8, which is titled "Move an assumption; move the cash line". The levers link to `/scenarios?...` instead of `/app/scenarios?...`. I need the levers to move the model in place, and I need the tour step that tells a presenter to move an assumption to survive being obeyed. 20 lever controls (volume, price, cost to serve, subcontract rate, collection days × 4 steps) plus all 5 "Run it" buttons on the saved scenarios point at the unprefixed path. Clicked "VOLUME −10%" on `/app/scenarios`: landed on `http://localhost:3002/scenarios?volume=0.9`, body text "404 — This page could not be found." Clicked "Run it" on "Revenue down 10%": same 404. `curl` confirms: `/scenarios?volume=0.9` → 404, `/app/scenarios?volume=0.9` → 200. Worst case, and the one a buyer will hit: opened `/?step=8&mode=guided&device=desktop`, which frames `/app/scenarios?view=inner&focus=section-effect&dsoDays=10` and captions it "Move an assumption; move the cash line … LOOK AT: the side-by-side effect and headroom comparison". Clicked "+4%" inside the frame. The product was replaced, inside its own device frame, by the Next.js 404 page. Screenshot at `/tmp/review-driver/user-tour-scenario-404.png`. The engine underneath is fine — reached by a hand-typed working URL it recomputes properly. This is 25 dead controls in front of a live model, and they are the only model controls in the product.
- `CRITICAL` **user** — The evidence chain's terminal "Source rows" table prints foreign-currency amounts with a sterling symbol, and the rows do not tie to the figure they are evidence for. I need every amount in that table to be in the same currency as the figure above it, or the row's own currency named in a column — and the rows to reconcile to the total. `/app/explore?drill=0%3A5` (Revenue › 2026-07, £12.4m consolidated). The provenance table above it is correct: External revenue 1,234,522,034 minor + Intercompany unmatched 4,800,000 minor = £12.393m. The "Source rows — 11 of them" table below it lists 5 + 0.910 + 2.1 + 0.947 + 10.4 + 2.1 + 1.3 + 0.460 + 0.763 + 0.169 = about £22.6m of revenue rows, all marked "£". The Gulf rows (£10.4m + £2.1m) are AED: divided by ~4.6 they come to Gulf's real £2.7m. The d365-eu rows are EUR, the netsuite-us rows USD. `packages/model/src/facts.ts:57` says facts are stored "in minor units of the entity's functional currency"; `web/app/app/explore/page.tsx:504` renders them with `formatValue(row.amountMinor, 'currency')`, which always prints the presentation symbol. The same table shows a `revenue_ic` source row of "£903k" beneath a stored `revenue_ic` input of £48k, unexplained. £903k is the gross intercompany figure; £48k is the unmatched residual. Presented side by side as "the rows behind this number", that is a 19x discrepancy an auditor stops at. The caption says the rows "terminate the drill spine" and are "shaped like ledger lines". They are, and a controller will read them. I cannot hand this to anybody.
- `CRITICAL` **user** — Year to Go states, as a fact and labelled "for the year", a comparison of seven months of actuals against a twelve-month budget. I need the two sides of any stated gap to cover the same window, or the window named in the sentence. "Largest single gap to budget for the year: Kestrel Manufacturing Ltd, £23.9m behind" and "Service contracts, £13.7m behind", on the risk and opportunity cards. Manufacturing's revenue is running at about £5.5m a month — £23.9m behind budget is a third of its year. It is not a miss: group actual YTD is £90.2m against an FY budget of £147.0m, a £56.8m arithmetic difference, and Manufacturing is ~42% of revenue. £56.8m × 0.42 = £23.9m. `packages/analysis/src/outlook.ts:494` builds `yearCtx` at the full fiscal-year scope and line 432 passes it to `contributorsFor` with `comparator: { id: 'budget' }`. The actual side has seven months of data in that window; the budget side has twelve. The governed table at the top of the same page says revenue lands £153.3m, "+£6.3m to budget, AHEAD". So the page tells me the group is ahead of budget and its largest entity is £23.9m behind, in the same breath. The disclaimer ("the two do not reconcile") excuses the exposure figure, not a comparison of unequal windows.
- `CRITICAL` **user** — The app cannot agree with itself on whether the £212k of unmapped accounts is inside reported profit. I need one answer, because it is the difference between a clean board note and a £212k understatement of EBITDA. Overview, HIGH finding "2 unmapped accounts, £212k at stake": "Until they are mapped that value is **outside** the reported profit and loss" (`packages/analysis/src/detectors.ts:811`). The Controls page repeats it: "A code that reaches the ledger without a mapping carries value outside the reported profit and loss." Performance, Operating expense table: "Unmapped operating expense £212k … Total operating expense £3.1m." The catalogue defines Operating expense as "staff cost + other operating expense + unmapped" and EBITDA as "gross profit − operating expense". `seed.ts:961` is `ebitda = grossProfit - staffCost - otherOpex - unmappedOpex`. The reconciliation gate passes *because* the £212k is on a P&L line: "Mapped P&L reconciles to the trial balance — PASSED — £212k trial-balance rows held on the unmapped P&L line." The catalogue note says the unmapped line "is the reason the mapped profit and loss ties to the trial balance." One of these is untrue. I read the Overview first, because that is where the demo starts.
- `CRITICAL` **user** — July's forecast comparator contains the same £212k of unmapped cost that only appeared in July's actuals, so the EBITDA variance nets it to zero. I need the unmapped line to show its full £212k adverse variance against a forecast that could not have contained it. Performance, Operating expense: "Unmapped operating expense — actual £212k, comparator £212k, £ variance +£0.00, % variance +0.0%". The EBITDA bridge draws a bar labelled "Unmapped opex £0k" — the app asserting these accounts contributed nothing to the miss. The Controls GL-code panel says both codes were created on 2026-07-03 and 2026-07-09. Forecast v6 has actuals through 2026-06. `seed.ts:945` states the intent in a comment: "the unmapped accounts land in July only, and only as actuals: a forecast cannot have failed to map an account that had not appeared when it was made." Line 948 gates them on `!healthy && month === SEED_END` only, never on `projecting`, so every forecast version carries them too. The consequence is on the headline KPI card: EBITDA "−3.0%, −£64k vs Forecast v6". The unmapped item alone is £212k. The published miss is a third of the real one.
- `HIGH` **user** — I cannot work the queue. The nine "items that need a decision" are the same nine whoever I am, the page misstates how many there are, and six of them cannot be opened. I need a queue filtered to what I own, with a date and a state on each item, and every item openable. `/app`, `/app?as=group-fpa`, `/app?as=group-controller`: the role label in the context strip changes; the nine findings, their order and their owners are byte-identical. I own one of them ("Subcontract labour under-called"). The other eight belong to the Operations Director, the Treasurer, the Commercial Director, the Sales Director and the Group Financial Controller, and each of them sees my item too. The summary line says "9 findings in Jul 2026: 3 adverse, 2 favourable, 3 risks and 1 opportunity", and the nav badge says 9. The boards themselves say "3 of 7 shown … 4 below the cut" and "3 of 5 shown … 2 below the cut". There are 15 findings. My queue size is misstated by two thirds. The six below the cut are a sentence in a paragraph — "Service contracts margin 267bps behind forecast; Kestrel Gulf collections 12 days slower; 1 of 5 ledgers not closed; 2026-06 restated after it was reported". I counted zero links, buttons or disclosure controls containing that text. No owner, no evidence, no way in. No item carries a due date, a state, or anything I can mark. There is nothing to tell me which of these needed me today, and nothing to record that I dealt with one.
- `HIGH` **user** — Two different thirteen-week cash forecasts, for the same month, the same group and the same approved version, on two pages, with different breach weeks and different shortfalls and no label saying they are different things. I need each cash line to name its opening basis, or one number. `/app/cash`: "Week 9 closes at £1.7m, £760k under the £2.5m floor. Its low point is £1.3m in week 10." Four breach weeks (9, 10, 11, 12). Transfer panel: "£7.9m can arrive in time against a shortfall of £760k." `/app/scenarios`, row labelled "Approved forecast": low point £2.2m, headroom −£273k, breach Week 10. Decision card: "The floor is breached in week 10 by £273k, and £8.8m can be reached in time." The gap is the opening balance — Cash opens on the £4.8m actual, Scenarios opens on Forecast v6's £5.9m — but neither page says so. As the person drafting the treasury paper I have two shortfalls, £760k and £273k, and no way to choose.
- `HIGH` **user** — The Explore grid's default forecast comparison reports every closed month as exactly on forecast, which flatly contradicts the Quality page in the same app. I need closed months compared to the forecast that was in force when they were open, or a note on the grid saying the comparator has absorbed the actuals. `/app/explore`, rows=measure, cols=period, comparator vs Forecast v6: revenue, EBITDA, cash and DSO all read "+0.0%" / "+0 days" for 2026-02, 03, 04, 05 and 06. Five of six columns. Verified in the raw CSV: `Revenue,2026-02, Actual raw 1247083317, Comparative raw 1247083317, Movement 0`. It is arithmetically correct — v6 has actuals through June — and it is the first thing I would build, and it says we hit forecast to the penny for five months. `/app/quality`, same session: "EBITDA — mean error +20.0%, 3 of 3, OVER-CALLED. Subcontract labour — −9.6%, 3 of 3, UNDER-CALLED. Cost of sales — 3 of 3, UNDER-CALLED." And value added versus a naive baseline of −328.5% on EBITDA. The one non-zero closed month, June's "+223bps" margin beat, is not a beat: it is the v-2026-07-restate-2026-06 reclassification. The June Overview explains that properly. The Explore grid and the Performance page, where I would actually meet it, say nothing.
- `HIGH` **user** — On this server the Ask panel is dead every single time, including all four of the questions the product itself puts on screen, and the README says the demo without a key "is whole". I need the suggested questions to be answerable in whatever mode is being shown, or the panel to say up front that it cannot answer before I type into it. Six POSTs to `/api/ask` (three of them the same flagship margin question): 6/6 `{"kind":"unavailable", ... "The answer service is not running here, so nothing could be looked up."}`. Four attempts through the UI textbox: 4/4 the same. Clicking the offered "Why is EBITDA ahead of forecast?" — refused. Nothing on the panel warns me first. It shows "Asking… / Working on your question." and then refuses. On the empty submission it left the previous question's refusal on screen. The refusal itself is well written and honest. But "an executive answer, an analyst drill and a controller evidence chain remain the same computation" is the product thesis on the front door, and one of the three cannot be shown at all here. That is not whole.
- `HIGH` **user** — The Performance page's entity table does not sum to the group and does not name the elimination, in the one place the deck promises it will. I need the intercompany elimination as a labelled row wherever entity rows sit under a group figure, or a note on that table saying the rows do not add. `/app/performance`, "Revenue by entity": group £12.4m, then Manufacturing £5.3m, Services £3.0m, Gulf £2.7m, Europe £1.5m, Inc £730k. The five sum to £13.2m. The £855k elimination is absent and there is no note. The Overview's equivalent table has the row ("Eliminations and unattributed") and a paragraph explaining why the parts do not add. The Explore drill does it best: "Intercompany eliminated −£855k … These parts sum to the cell exactly." Deck slide 6: "the consolidated total split into its five entities, with the intercompany elimination named as its own line." The drill honours that; the analyst page I would live on does not.
- `HIGH` **user** — Percentage movements are published across a sign change and on negative bases, in a product that is otherwise scrupulous about refusing meaningless ratios. I need a sterling movement and a suppressed percentage where the comparative is zero or of the opposite sign. `/app?entity=services`: "EBITDA −£175k, −141.4% vs Jul 26 Forecast v6". The comparative is −£73k; the movement is £102k worse. −141.4% is not a rate of anything. Same card: "CASH −£1.6m, −8.6%" on a negative balance, where I cannot tell from the sign whether the overdraft grew or shrank. Overview, EBITDA by entity: shares of "−179.7%", "159.9%", "110.7%" on a net movement the page itself says is "largely cancelling". The gross-margin table correctly refuses to show a share for the same reason. The rule is applied in one place and not the other.
- `HIGH` **deck** — There is no value claim anywhere in the deck. Not a quantified one, not even a directional one. The problem slide says "Twelve times a year, the same work" and no later slide claims that work gets smaller, faster, cheaper or safer. The verbs on the product slides are "opens", "names", "drills", "reaches" — all about mechanism, none about outcome. I would have to see, on the face of the deck, what changes for the finance function: days off the close, questions answered without a rebuild, people not doing the rebuild, arguments that stop. Any one of them, stated as a claim I could hold you to. The only outcome numbers in the whole deck (72h→15min, 67%→95%, 2,000+ users) belong to a different product for a different client in a different industry. They are on the credentials slide, not the product slides, and a careful reader notices that the product itself is described entirely without numbers of its own.
- `HIGH` **deck** — Nothing in the deck says the workbench has ever run on real finance data. Every figure on slides 4, 5 and 6 comes from "a synthetic group" — which the deck honourably discloses — and the twelve-week plan then puts "back-tested against closes your controllers already stood behind" at weeks 4–8. Read plainly, that says the first real close this thing is measured against is mine. There is no reference customer for this product, no pilot, no "we did this at X and it reproduced eleven of twelve months". I would have to see either one named engagement where the workbench ran on a real ledger and what it found, or an explicit sentence owning that I would be the first and saying what protects me if week 8 fails.
- `HIGH` **deck** — The deck's flagship promise is never shown. The title tagline is "Every figure in your close, opened to the rows that made it"; slide 6's headline is "Each figure drills to the rows that made it". The screenshot on slide 6 stops at the five entities, and the panel that would hold the rows — "Technical evidence: Formula, provenance and source rows" — is shut, which the deck's own alt text concedes ("closed until it is opened"). Not one ledger row appears anywhere in the deck. The single capability the whole argument rests on is the one thing the reader is asked to take on trust. I would have to see the evidence panel open, with real (synthetic is fine) rows, accounts and vintages visible on the slide.
- `HIGH` **deck** — The deck contradicts itself about what the product actually takes off my desk. The "Why Now" slide puts variance, forecast, scenario and "the commentary a board reads" in the **Decision** layer, tagged **"Where the work is"** and glossed "still a person, every month" — and puts the workbench in the layer *below* it, tagged "Missing". The standfirst on that same slide says the gap is to "connect the pieces, **interpret the movement and explain it to a board**". So: does the workbench write the commentary or not? Its own diagram says no; its own prose says yes; and the screenshot on the very next slide contains written interpretation ("Because of Kestrel Manufacturing Ltd, up £285k — 46.1% of the movement, owned by UK Financial Controller"). Commentary is the most expensive thing in my month, and after reading the deck twice I still cannot tell you whether you claim to produce it. I would have to see one consistent statement of what the layer does and what stays human, with the diagram and the prose agreeing.
- `HIGH` **deck** — The commitment behind the ask is disclosed only as a caption on a slide about something else. "From US$185K · one workflow, live" sits in the middle card of the engagement slide; the ask slide names no money at all. And US$185K is only the build. Nothing tells me what the workbench costs to keep: licence or subscription, per user or per entity, who hosts it, what year two looks like, or what "from" is a function of. I am being asked to start a path whose total cost I cannot bound. I would have to see, at minimum, the twelve-week price and the run cost after it, on the same page as the ask.
- `HIGH` **deck** — The deck asks for five ledgers of transaction-level data and never says where that data goes. No deployment model (my tenant, your cloud, on-premise), no data residency, no statement of who at Deeplight can see GL rows, no mention of what happens to my data if we stop at week 3. The group depicted spans the UK, Germany, the US and the Gulf, and your own credentials are regulated in the UAE, so this is the first question my CIO asks and the second my auditor asks. "The controls your audit function asks for" is named as a week-9 deliverable, which is after my data has been in your hands for eight weeks. I would have to see one line on where it runs and one on who can see it.
- `HIGH` **deck** — "Why Now" does not answer why now. The headline under it — "Keep the accounting system. The gap is the layer above it." — answers *where this fits*, which is a different and already-made point. Nothing in the deck dates anything: no change in technology, cost, regulation or expectation that makes this possible in 2026 and not 2022. The only "now" is the word itself, in "the work a governed layer on top can **now** take on", which asserts the claim rather than supporting it. I would have to see what changed. Absent that, the honest answer to "why not next year" is "no reason given".
- `HIGH` **deck** — Three of twelve surfaces are shown and the other nine are never named. The title slide sells "three of the twelve surfaces running live"; the contents page promises "Roadmap"; the slide that arrives under that name is the twelve-week delivery plan. So the deck claims a product four times larger than what it shows, and then never tells me what the other three quarters are or when I would get them. Either name the twelve, or drop the number and claim only what you show.
- `LOW` **deck** — The deck's proudest number collides with the adjacent screenshot. Slide 5 leads with "**£0k** unattributed", while the screenshot on slide 4 carries a line reading "Eliminations and unattributed +£17k 2.8%" and a note that "The other 2 are in the remainder". Both are defensible in their own frames — bridge residual versus entity remainder — but the deck uses one word for two things on facing pages, and the reader who notices concludes the £0k is a definitional choice rather than an achievement.
- `LOW` **deck** — The structure the contents page promises is not the structure that arrives. The contents lists four numbered sections (01 Problem, 02 Solution, 03 Company, 04 The Ask) and five sub-items; both dividers in the deck are numbered **03**, so "The Solution" arrives labelled 03 when the contents calls it 02. Two promised sub-items, "Our Team" and "Engagement", never appear as a section name — the team is folded into "The Company" and the engagement slide's eyebrow reads "How We Work", a name the contents page does not contain. A reader navigating by section name cannot find two of the nine things they were promised.
- `LOW` **deck** — Slides 4 and 6 make the same claim twice. "Every figure on this screen opens into the working behind it" and "Each figure drills to the rows that made it" are one proposition, and with the tagline that is three statements of traceability out of eleven slides. One of those two slots would be better spent on the value claim or the proof that is missing above.
- `LOW` **deck** — A grammatical error is baked into a screenshot: "not every posting carries a entity", in the note under the revenue-by-entity table on slide 4. `deck lint` cannot see inside a JPEG, so I am flagging it here.

_0 of 5 returned yes — 14 critical · 32 high · 7 low. Fix what blocked, then run the next round with reviewers who have not seen this one._

## Round 3 — 2026-08-17 — 9bac57e

| Reviewer | Verdict |
| --- | --- |
| cpo | — no reply yet |
| ceo | — no reply yet |
| domain | — no reply yet |
| user | — no reply yet |
| deck | — no reply yet |

_Open: 0 of 5 replies recorded. A round with a missing verdict is not a round._

## Round 4 — 2026-08-18 — 9ece9fd

| Reviewer | Verdict |
| --- | --- |
| sponsor | yes |
| operator | yes |
| blocker | no |

Blocking gaps:

- `HIGH` **sponsor** — The opening spends the only two guaranteed slides on Deeplight and an agenda, so I nearly stopped before seeing a problem I recognised. Put the control-environment failure on slide one—the AI pilot that cannot survive audit, integration, or a normal Tuesday—so I see my own stalled programme before deciding whether to keep reading.
- `HIGH` **operator** — The twelve-week Accelerate story quietly makes client-side readiness look schedulable: access to production-like data, data-owner and security approval, source-system interfaces and remediation appear to fit inside the opening evidence phase, but in my shop those cross-team dependencies can absorb months before engineers can build. In the meeting I would need to hear the entry conditions for the clock, what happens when a client dependency misses them, and one example where the team reached production through material access or data-quality delay rather than around it.
- `HIGH` **blocker** — The pitch turns senior access into unbounded key-person risk. I would need a credible view of who actually staffs and supports an engagement beyond the four named principals, including continuity when one is unavailable and capacity to run concurrent client programmes.

Kept from this section, unchanged:

_Scope: the synced Deeplight reference deck at `/reference.html` only. The Finance Workbench
product and its product-specific deck are outside this round._

_Round 4: 2 of 3 returned yes — 0 critical · 3 high · 0 low._
