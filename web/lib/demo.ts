/**
 * Who this demo is, in one place.
 *
 * This file is the scaffolder's substitution surface: `create-demo` rewrites these five
 * values when it copies the template and touches nothing else. Everything below is a plain
 * string constant for exactly that reason — a placeholder syntax like `{{name}}` would make
 * the template unbuildable, and a template that cannot be built is a template nobody can
 * check before shipping it.
 *
 * Changing the seed changes the world. That is the intended way to reshuffle a demo's
 * figures; nothing else in the app may.
 */

/** The full name: the browser title, the manifest, the gate heading. */
export const DEMO_NAME = 'Dl Finance Workbench';

/** What fits under a home-screen icon — about 12 characters. */
export const DEMO_SHORT_NAME = 'Dl';

/** Two or three characters. A demo's mark is type, not an image file. */
export const DEMO_MARK = 'DFW';

/** One line, used by the manifest and the gate. */
export const DEMO_DESCRIPTION =
  'Dl Finance Workbench — a demonstration build. Every figure below is generated from a fixed seed.';

/** The seed the whole world is a pure function of. */
export const DEMO_SEED = 'dl-finance-workbench';

/** The mark's colours. Used by the drawn icon and the manifest, so they cannot drift apart. */
export const DEMO_ICON_BACKGROUND = '#0d3b5e';
export const DEMO_BACKGROUND_COLOR = '#e8edf3';
